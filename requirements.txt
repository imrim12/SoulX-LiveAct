# torch/torchaudio/torchvision intentionally not pinned here: the Docker base image
# (pytorch/pytorch:2.8.0-cuda12.8-cudnn9-devel) already provides the matching cu128
# build; reinstalling via pip risks resolving a different (non-cu128 or CPU-only)
# build from PyPI's generic index instead.
opencv-python>=4.11.0.86
diffusers>=0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.12.1
tqdm
imageio
easydict
ftfy
dashscope
imageio-ffmpeg
scikit-image
loguru
gradio>=5.0.0
numpy
xfuser==0.4.5
pyloudnorm
scenedetect
moviepy==1.0.3
decord